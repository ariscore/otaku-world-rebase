# TODO: Add custom recommendation logic
query GetRecommendedAnime($page: Int) {
    Page(perPage: 30, page: $page) {
        pageInfo {
            hasNextPage
        }
        media(sort: [POPULARITY_DESC], type: ANIME, isAdult: false) {
            ...MediaShort
        }
    }
}