query GetActivities($page: Int, $type: ActivityType) {
    Page(page: $page, perPage: 30) {
        pageInfo {
            hasNextPage
        }
        activities(sort: [ID_DESC], type: $type, type_in: [ANIME_LIST, TEXT]) {
            ...TextActivity
            ...MessageActivity
            ...ListActivity
        }
    }
}